# Copilot Instructions — ComfyUI Asset Manager

프로젝트의 상세 규칙과 보안·IPC·테스트 컨벤션은 `AGENTS.md`와 `SKILL.md`를 따른다.

## Build & Dev Commands

```bash
npm run dev
npm run build
npx electron-vite build
npm test
npm run test:watch
npm run test:coverage
npm run lint
npm run format
npm run typecheck
```
