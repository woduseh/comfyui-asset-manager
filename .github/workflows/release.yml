name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: '.node-version'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify release metadata
        run: npm run verify:release -- "${GITHUB_REF_NAME}"

      - name: Verify
        run: npm run verify:coverage

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: release-verification
          path: |
            .reports/verify/
            coverage/
          include-hidden-files: true
          retention-days: 7

  build-windows:
    needs: lint-and-test
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: '.node-version'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Windows distributables
        run: npm run dist:all
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: windows-distributables
          path: |
            dist/*.exe
          retention-days: 5

  create-release:
    needs: build-windows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download Windows artifacts
        uses: actions/download-artifact@v8
        with:
          name: windows-distributables
          path: dist/

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Generate release notes from CHANGELOG
        id: changelog
        run: |
          # Extract the section for this version from CHANGELOG.md
          VERSION="${{ steps.version.outputs.VERSION }}"
          NOTES=$(sed -n "/^## \[${VERSION}\]/,/^## \[/p" CHANGELOG.md | head -n -1)
          if [ -z "$NOTES" ]; then
            echo "Missing CHANGELOG section for ${VERSION}" >&2
            exit 1
          fi
          # Write to file for multiline support
          echo "$NOTES" > release-notes.md

      - name: Generate SHA256 checksums
        run: |
          cd dist
          ls -la
          mapfile -t exe_files < <(find . -maxdepth 1 -type f -name '*.exe' | sort)
          if [ "${#exe_files[@]}" -eq 0 ]; then
            echo "No .exe files found in dist/" >&2
            exit 1
          fi
          sha256sum "${exe_files[@]}" > checksums-sha256.txt
          cat checksums-sha256.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          name: ${{ github.ref_name }}
          body_path: release-notes.md
          files: |
            dist/*.exe
            dist/checksums-sha256.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
